import { CustomerBffEntity } from '../entities/customer-bff.entity';
import { logger } from '../utils/logger';

// Implementacion de funciones
export const get = async (): Promise<CustomerBffEntity[]> => {
    return list;
}
